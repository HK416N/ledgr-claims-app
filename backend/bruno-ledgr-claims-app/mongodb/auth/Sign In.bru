meta {
  name: Sign In
  type: http
  seq: 1
}

post {
  url: {{MONGODB_BASE_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "max@example.com",
    "password": "Password123"
  }
}
